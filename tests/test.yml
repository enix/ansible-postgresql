---

- hosts: test
  tasks:
  - apt:
      name:
        - gnupg
        - locales
      state: present
      update_cache: yes

- include: test_stretch.yml
  when: ansible_distribution_release == "stretch"

- include: test_jessie.yml
  when: ansible_distribution_release == "jessie"
